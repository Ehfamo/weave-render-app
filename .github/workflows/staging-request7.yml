name: XEOMX Stage5.4 Exact Golden Gate

on:
  push:
    branches:
      - verify/stage5-4-exact-golden-20260815
  workflow_dispatch:

permissions:
  contents: read

jobs:
  registration-probe:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Confirm isolated workflow trigger
        run: |
          echo "STAGE54_WORKFLOW_REGISTERED=1"
          echo "HEAD=${GITHUB_SHA}"
