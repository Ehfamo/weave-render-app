# P2 source closure

Starting HEAD: `7dd2d9de3fcf57a210893b0ec6572c6cd212eaf0`

P2 now connects risky-step continuation to a durable server authority. Existing Stage 5.3 `controlled_runs`, `approval_requests`, exact-project-owner RLS/trigger decisions and control-plane audit remain canonical. The additive source migration adds atomic, service-only, one-time approval consumption, verifies task/execution/step/tool/project, expiry and queued state, and exposes no client execution grant.

The Coding Agent execute boundary accepts an exact approved `PatchProposal`, reauthorizes user/project/workspace, restricts paths, blocks traversal and secret paths, requires expected target text, limits files/bytes, rejects malformed/duplicate changes and runs only registered command IDs. It exposes neither arbitrary filesystem access nor shell strings.

Fresh validation after the production build generated its expected artifacts: P2 focused and closure tests PASS; full suite `200/200 PASS`, zero skipped/cancelled; Typecheck PASS; Lint PASS with zero errors and nine existing warnings; Build PASS using isolated localhost Supabase URL and dummy public test value; diff check PASS. No production migration was applied. Main, Production, Production Supabase, Cloudflare, DNS, secrets, payments and deployment were untouched.

`LIVE_MODEL`, `LIVE_RESEARCH` and `LIVE_BROWSER` remain separately deferred external execution classifications and do not block P2 source closure.

P2_APPROVAL = PASS

P2_CODING_AGENT = PASS

P2 = CLOSED

P3_STARTED = NO
